# Instalation

```bash
$ yarn install

#$ yarn add jquery@2.1.1 @types/jquery jquery-sparkline --dev

$ yarn global add typings

#$ typings install --global --save file:./ClientApp/typings/sparkline/index.d.ts

$ typings install
```